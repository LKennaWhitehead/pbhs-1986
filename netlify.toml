[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: React Router handles /store, /donate, /events, etc.
# without this, direct navigation or refresh on those routes 404s.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
